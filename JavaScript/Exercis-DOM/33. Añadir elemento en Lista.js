// Return element list
let elementList = document.querySelector('ol');

// Add new element with first position
if (elementList) {
    newElement.insertAdjacentHTML('afterbegin', '<p> primer parrafo </p>');
}