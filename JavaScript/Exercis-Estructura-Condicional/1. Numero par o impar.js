/* Enter a number */
let number = Number(prompt("Ingrese un numero "))

/* Say if is par or impar number */
if (number === 2 || number === 4 || number === 6 || number === 8 || number === 10) {
    console.log(`El numero ${number} es par`)
} else if (number === 3 || number === 5 || numebr === 7 || number === 9) {
    console.log(`El numero ${number} es impar`)
}